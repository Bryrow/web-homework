query GetTransactions {
    transactions {
        id
        user_id
        description
        merchant_id
        debit
        credit
        amount
    }
}

mutation DeleteTransaction ($id: ID!) {
    deleteTransaction(id: $id) {
        id
    }
}
